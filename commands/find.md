# `find`
searches for directories and files that meet specified criteria

## options
* `-name`: a specific string to search for that must be enclosed in quotes
* `-iname`: same as `-name`, but case-insensitive
* `-mtime`: used for finding files or directories last modified within a certain timeframe (days-based)
* `-mmin`: similar to `-mtime`, but minutes-based