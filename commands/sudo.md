# `sudo`
temporarily grants elevated permissions to specific users
## notes
* short for "**s**uper-**u**ser-**do**"
* allows users to run every command with root privileges without signing in as root user
* running sudo prompts user to enter the password for the currently logged in user
* users must be granted sudo access through **sudoers** file
