# `ls`
displays the names of files and directories in the current working directory
## notes
* can specify multiple options in a single `ls` command execution
* e.g., `$ ls -la`
## options
`-l` displays permissions to files and directories
`-a` displays hidden files
`-r` displays the files and directories in reverse order
`-t` sort by time, displays newest first
