# `/home`
each user in the system gets their own home directory
# `/bin`
"binary" for short, contains binary files and other executables
# `/etc`
contains system's configuration files
# `/tmp`
stores many temporary files, commonly used by attackers because anyone in the system can modify data in these files
# `/mnt`
"mount" for short, stores media like hard drives and USB drives